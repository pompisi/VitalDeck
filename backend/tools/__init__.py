"""tools — dev-only helpers (synthetic data generation + end-to-end seeding).

these never ship to the Pi pipeline; they exist so the whole backend can be
exercised without a real ring capture. importing the package is cheap.
"""
from __future__ import annotations
