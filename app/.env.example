# Copy this to `.env` (gitignored) and set your backend's base URL. It becomes the
# app's default backend until you change it in the in-app SET tab. Leave blank to
# require in-app configuration on first launch.
VITALDECK_API_URL=
